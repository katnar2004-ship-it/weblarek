# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Интерфейсы данных
#### IProduct (Товар)
Описывает структуру объекта товара, который приходит с сервера и используется внутри приложения для отображения в каталоге, корзине и карточке товара.

interface IProduct {
  id: string; - уникальный идентификатор товара
  description: string; - подробное описание товара
  image: string; - ссылка на изображение товара
  title: string; - название товара
  category: string; - категория товара
  price: number | null; - цена товара
}
#### IBuyer (Покупатель)
Описывает структуру данных покупателя, которые он вводит при оформлении заказа.

interface IBuyer {
  payment: TPayment; - способ оплаты
  email: string; - электронная почта
  phone: string; - номер телефона
  address: string; - адрес доставки
}

### Модели данных
В этом разделе описаны классы, отвечающие за хранение и управление данными в приложении.

#### CatalogModel (Каталог товаров)

Назаначние и зона ответственности: хранит массив всех товаров, а также товар, выбранный для подробного отображения.

Конструктор: не принимает параметров.

Поля класса:
`_products` - тип: IProduct[]; приватное поле. Хранит массив товаров, полученных с сервера.
`_previewProduct` - тип: IProduct | null; приватное поле. Хранит товар, выбранный для подробного отображения (null, если товар не выбран).

Методы класса:
`setProducts(products: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода.
`getProducts(): IProduct[]` - получение массива товаров из модели.
`getProductById(id: string): IProduct | undefined` - получение одного товара по его id (undefined, если товар не найден).
`setPreview(product: IProduct): void` - сохранение товара для подробного отображения.
`getPreview(): IProduct | null` - получение товара для подробного отображения.

#### BasketModel (Корзина)

Назаначние и зона ответственности: хранит массив товаров, выбранных покупателем для покупки.

Конструктор: не принимает параметров.

Поля класса:
`_items` - тип: IProduct[]; приватное поле. Хранит массив товаров в корзине.

Методы класса: 
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине.
`addItem(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины.
`removeItem(productId: string): void` - удаление товара, полученного в параметре из массива корзины.
`clear(): void` - очистка корзины.
`getTotalPrice(): number` - получение стоимости всех товаров в корзине.
`getItemCount(): number` - получение количества товаров в корзине.
`hasItem(productId: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### BuyerModel (Покупатель)

Назаначние и зона ответственности: хранит данные покупателя (способ оплаты, адрес, телефон, email) и управляет ими.

Конструктор: не принимает параметров.

Поля класса:
`_payment` - тип: TPayment | null; выбранный способ оплаты (card или cash).
`_address` - тип:string; адрес доставки.
`_phone` - тип:string; номер телефона.
`_email` - тип:string; электронная почта.

Методы класса:
`setBuyerField(field: Partial<IBuyer>): void` - сохраняет одно или несколько полей покупателя. Параметр — объект, содержащий только те поля, которые нужно обновить. Позволяет сохранить только адрес или только телефон, не удаляя значения других полей.
`getBuyerData(): IBuyer` - получение всех данных покупателя.
`clear(): void` - очистка данных покупателя.
`validate(): Partial<Record<keyof IBuyer, string>>` - проверяет валидность каждого поля. Правила валидации: Поле является валидным, если оно не пустое. Возвращаемое значение: Объект, в котором ключи — это названия полей с ошибками, а значения — текст ошибки. Если поле валидно, оно отсутствует в возвращаемом объекте.

### Слой коммуникации
Класс `AppApi` отвечает за взаимодействие с сервером «веб-ларёк». Он использует композицию с базовым классом `Api` для выполнения HTTP-запросов.

Назначение и зона ответственности: получение списка товаров с сервера (GET-запрос на эндпоинт `/product/`), отправка данных о заказе на сервер (POST-запрос на эндпоинт `/order/`).

Зависимости: использует экземпляр класса `Api` для выполнения HTTP-запросов, работает с интерфейсами типов из `types/index.ts`.

Конструктор: `constructor(api: IApi)`; параметры: `api: IApi` - объект, соответствующий интерфейсу `IApi`, который предоставляет методы `get` и `post` для работы с сервером.

Методы класса:
`getProducts(): Promise<IProductResponse>` - выполняет GET-запрос на эндпоинт `/product/` для получения списка товаров. Возвращает `Promise<IProductResponse>` - промис с объектом, содержащим массив товаров.
`postOrder(order: IOrder): Promise<IOrderResponse>` - выполняет POST-запрос на эндпоинт `/order/` для отправки данных о заказе. Пармаетры: `order: IOrder` - объект с данными заказа (товары, данные покупателя, способ оплаты). Возвращает `Promise<IOrderResponse>` - промис с объектом, подтверждающим покупку (содержит общую сумму заказа).

### Модели представления
В этом разделе описаны классы, которые необходимы для отображения сайта.

#### Класс Card
Базовый класс для всех типов карточек товара. Содержит общую логику работы с карточкой. Класс является дженериком и принимает в переменной `T` тип данных.

Зона ответственности: хранение общих полей и методов для карточек в каталоге, preview и корзине.

Поля класса:
`_id` - тип: string; идентификатор товара
`titleElement` - тип: HTMLElement; элемент заголовка
`priceElement` - тип: HTMLElement; элемент цены
`categoryElement?` - тип: HTMLElement; элемент категории (опционально)
`imageElement?` - тип: HTMLImageElement; элемент изображения (опционально)

Методы класса: 
`set id(value: string): void` - установить ID товара
`get id(): string` - получить ID товара
`set title(value: string): void` - установить заголовок
`set price(value: number | null): void` - установить цену
`set category(value: string): void` - установить категорию
`set image(value: string): void` - установить изображение

Генерируемые события:
`card:select` - при клике по карточке (передаёт id товара)

#### Класс CardCatalog
Карточка товара для отображения в каталоге (галерее).

Поля класса:
`button` - тип: HTMLButtonElement; кнопка выбора товара

Методы класса:
Наследует все методы от `Card<T>`

Генерируемые события:
`card:select` - при клике по карточке (передаёт id товара)

#### Класс CardPreview
Карточка товара для подробного просмотра (preview) в модальном окне.

Поля класса: 
`descriptionElement` - тип: HTMLElement;	элемент описания товара
`button` - тип: HTMLButtonElement; кнопка "В корзину"

Методы класса:
`set description(value: string): void` - установить описание
`set buttonText(value: string): void` - установить текст кнопки
`disableButton(disabled: boolean): void` - включить/выключить кнопку

Генерируемые события:
`card:add-to-basket` - при клике на кнопку "В корзину" (передаёт id товара)

#### Класс CardBasket
Карточка товара для отображения в корзине.

Поля класса:
`indexElement` - тип: HTMLElement;	элемент с порядковым номером
`deleteButton` - тип: HTMLButtonElement;	кнопка удаления из корзины

Методы класса: 
`set index(value: number): void` - установить порядковый номер

Генерируемые события:
`card:remove-from-basket` - при клике на кнопку удаления (передаёт id товара)

#### Класс Form
Базовый класс для всех форм. Содержит общую логику валидации и управления состоянием формы. Класс является дженериком и принимает в переменной `T` тип данных.

Зона ответственности: Обработка ввода данных, валидация, управление кнопкой отправки.

Поля класса:
`form` - тип: HTMLFormElement; DOM-элемент формы
`submitButton` - тип: HTMLButtonElement; кнопка отправки формы
`errorsContainer` - тип: HTMLElement; контейнер для отображения ошибок
`_data` - тип: T;	приватное поле с данными формы

Методы класса:
`setData(data: Partial<T>): void` - частичное обновление данных формы
`getData(): T` - получение всех данных формы
`validate(): boolean` - валидация формы (возвращает true, если все поля валидны)
`setErrors(errors: Partial<Record<keyof T, string>>): void` - отображение ошибок валидации
`clear(): void` - очистка формы
`disableSubmit(disabled: boolean): void` - включить/выключить кнопку отправки

Генерируемые события:
`form:input-change` - при изменении любого поля (передаёт имя поля и значение)
`form:submit` - при отправке формы (передаёт все данные формы)

#### Класс FormOrder
Форма для ввода адреса и выбора способа оплаты.

Поля класса:
`addressInput` - тип: HTMLInputElement; поле ввода адреса
`cardButton` - тип: HTMLButtonElement; кнопка выбора оплаты картой
`cashButton` - тип: HTMLButtonElement; кнопка выбора оплаты наличными

Методы класса:
`set payment(value: TPayment | null)` - установить способ оплаты
`get payment(): TPayment | null` - получить способ оплаты
`set address(value: string)` - установить адрес
`get address(): string` - получить адрес
`validate(): boolean` - проверяет заполненность адреса и выбран ли способ оплаты

Генерируемые события:
`order:payment-change` - при выборе способа оплаты
`order:address-change` - при вводе адреса

#### Класс FormContacts
Форма для ввода контактных данных (email и телефон).

Поля класса:
`emailInput` - тип: HTMLInputElement поле ввода email
`phoneInput` - тип: HTMLInputElement поле ввода телефона

Методы класса:
`set email(value: string)` - установить email
`get email(): string` - получить email
`set phone(value: string)` - установить телефон
`get phone(): string` - получить телефон
`validate(): boolean` - проверяет корректность email и телефона

Генерируемые события:
`contacts:email-change` - при вводе email
`contacts:phone-change` - при вводе телефона

#### Класс Modal
Компонент модального окна. Не имеет дочерних классов. Отвечает только за открытие/закрытие и отображение контента.

Зона ответственности: Управление отображением модального окна, обработка кликов по оверлею и кнопке закрытия.

Поля класса:
`modalContainer` - тип: HTMLElement;	контейнер модального окна
`closeButton`	- тип: HTMLButtonElement; кнопка закрытия
`contentContainer` - тип: HTMLElement;	контейнер для динамического контента

Методы класса:
`open(): void` - открыть модальное окно
`close(): void` - закрыть модальное окно
`setContent(content: HTMLElement): void` - установить содержимое модального окна
`clearContent(): void` - очистить содержимое

Генерируемые события:
`modal:open` - при открытии модального окна
`modal:close` - при закрытии модального окна

#### Класс Header
Компонент шапки сайта. Отображает логотип и счётчик корзины.

Поля класса:
`basketButton` - тип: HTMLButtonElement; кнопка корзины
`counterElement` - тип: HTMLElement; элемент счётчика товаров в корзине

Методы класса:
`setCounter(value: number): void` - установить значение счётчика

Генерируемые события:
`header:basket-click` - при клике на кнопку корзины

#### Класс Gallery
Компонент галереи (каталога) товаров. Отображает список карточек.

Поля класса:
`container` - тип: HTMLElement; контейнер для карточек

Методы:
`setCatalog(items: HTMLElement[]): void` - отобразить массив карточек

Генерируемые события: (не генерирует, только отображает)

#### Класс Basket
Компонент корзины. Отображает список выбранных товаров и общую сумму.

Поля класса:
`listContainer`	- тип: HTMLElement; контейнер для списка товаров
`orderButton`	- тип: HTMLButtonElement; кнопка "Оформить"
`priceElement` - тип: HTMLElement; элемент отображения общей суммы

Методы класса:
`setItems(items: HTMLElement[]): void` - отобразить массив карточек корзины
`setPrice(value: number): void` - установить общую сумму
`setButtonState(disabled: boolean): void` - включить/выключить кнопку оформления

Генерируемые события:
`basket:order` - при клике на кнопку "Оформить"

#### Класс OrderSuccess
Компонент уведомления об успешном оформлении заказа.

Поля класса:
`closeButton`	- тип: HTMLButtonElement;	кнопка закрытия
`totalElement` - тип: HTMLElement;	элемент отображения списанной суммы

Методы класса:
`setTotal(value: number): void` - установить списанную сумму

Генерируемые события:
`success:close` - при клике на кнопку "За новыми покупками"

### Презентер
Презентер — это слой приложения, который связывает модели данных и компоненты представления (View). Он отвечает за всю бизнес-логику работы страницы: обработку действий пользователя, обновление данных в моделях и синхронизацию интерфейса. Так как приложение имеет только одну страницу, используется один презентер. Код презентера находится в основном скрипте `main.ts` и не выносится в отдельный класс.

Зоны ответственности:
Инициализация - создание экземпляров всех компонентов (модели, API, View)
Загрузка данных - получение товаров с сервера и сохранение в модели каталога
Обработка событий - подписка на события от компонентов View и моделей данных
Синхронизация - обновление View при изменении данных в моделях
Навигация - управление модальными окнами и потоками оформления заказа
Отправка заказа - сбор данных из моделей, отправка на сервер, обработка ответа

События от компонентов View:
`card:select` - источник: `CardCatalog`; открыть preview товара
`card:add-to-basket` - источник: `CardPreview`; добавить товар в корзину
`card:remove-from-basket` - источник: `CardBasket`; удалить товар из корзины
`header:basket-click` - источник: `Header`; открыть корзину в модальном окне
`basket:order` - источник: `Basket`; открыть форму заказа
`order:submit` - источник: `FormOrder`; перейти к форме контактов
`contacts:submit` - источник: `FormContacts`; отправить заказ на сервер
`order:payment-change` - источник: `FormOrder`; обновить `BuyerModel.payment`
`order:address-change` - источник: `FormOrder`; обновить `BuyerModel.address` `contacts:email-change` - источник: `FormContacts`; обновить `BuyerModel.email`
`contacts:phone-change` - источник: `FormContacts`; обновить `BuyerModel.phone`
`modal:close` - источник: `Modal`; очистить контент модального окна
`success:close` - источник: `OrderSuccess`; закрыть модальное окно

События от моделей данных:
`catalog:changed` - источник: `CatalogModel`; обновить галерею товаров
`catalog:preview-changed` - источник: `CatalogModel`; отобразить preview товара
`basket:changed` - источник: `BasketModel`; обновить счётчик в Header, кнопки в Basket
`buyer:changed` - источник: `BuyerModel`; обновить поля в активных формах

https://github.com/katnar2004-ship-it/weblarek

